This folder holds concise, developer-focused docs for AntiProxy-SIH.

- `db_schema.md` — database schema overview (PostgreSQL).
- `frontend_student.md` — how the student app talks to the backend/DB.
- `frontend_teacher.md` — how the teacher app talks to the backend/DB.

