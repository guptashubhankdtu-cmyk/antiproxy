# AIMS Backend Environment Variables
# Copy this file to .env and fill in the values
# OR use the .secrets file which has all values pre-filled

# Database Configuration
# For local development (Docker):
DATABASE_URL=postgresql://aims:aims_pass@localhost:5433/attendance

# For GCP Cloud SQL (Production):
# DATABASE_URL=postgresql://shubhank165:shubhank@165google@35.244.49.80:5432/db-antiproxy

# For Cloud Run (with socket connection):
# DATABASE_URL=postgresql://shubhank165:shubhank@165google@/db-antiproxy?host=/cloudsql/anti-proxy-db:asia-south1:sih-postgres

# JWT Configuration
JWT_SECRET=your-jwt-secret-minimum-32-characters-long
JWT_EXPIRATION_HOURS=1

# Google OAuth
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Application Settings
DEBUG=false
CORS_ORIGINS=["*"]

# Azure Blob Storage (Optional - for file uploads)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_NAME=aimsattendanceapp
